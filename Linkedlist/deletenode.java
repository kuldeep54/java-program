package Linkedlist;
// //Can we delete a node guven the node
// itself as parameter?
// Is there any efficient way, provided that the given
// node is not the last node?

// delete node in a linket list 
public class deletenode {

    class ListNode{
        int val;
        ListNode next;
    ListNode(int x){
        val=x;
    }
    }
    
}
